import {UserStorageManager} from '../../storage/userStorageManager'
import {FileSystemDataStorage} from '../../storage/dataStorage'

describe('UserStorageManager', ()=>{
    describe('generateTreeData', ()=>{
        it('common', ()=>{
            // const storage = new FileSystemDataStorage()
            // const ins = new UserStorageManager('/root/tmp/test', storage)
            // const treeData = ins.generateTreeData('/')
        })
    })
})

